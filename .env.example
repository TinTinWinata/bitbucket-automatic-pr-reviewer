# Claude Model (haiku, sonnet, or opus)
CLAUDE_MODEL=sonnet

# Server Port (optional, defaults to 3000)
PORT=3000

# Bitbucket Authentication (choose one method)
# Option 1: Use App Password or Personal Access Token (recommended)
BITBUCKET_TOKEN=your-token
BITBUCKET_USER=your-username

# Option 2: Use Username and Password (alternative)
# BITBUCKET_USER=your-username
# BITBUCKET_PASSWORD=your-password

# Bitbucket Webhook Security
# BITBUCKET_WEBHOOK_SECRET=your-webhook-secret-here
# ALLOWED_WORKSPACE=your-workspace-slug